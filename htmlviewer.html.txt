<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Just a Tiiny Emulator</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #121212;
      color: white;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      height: 100vh;
      text-align: center;
    }

    h1 {
      font-size: 28px;
      margin-top: 60px;
    }

    .button-container {
      display: flex;
      flex-direction: column;
      gap: 15px;
      margin-top: 30px;
    }

    button {
      padding: 15px 30px;
      font-size: 18px;
      border: none;
      border-radius: 10px;
      background: #1e90ff;
      color: white;
      cursor: pointer;
      transition: background 0.3s;
    }

    button:hover {
      background: #0d74d1;
    }

    #backBtn {
      position: absolute;
      top: 0;
      left: 0;
      background: transparent;
      color: white;
      font-size: 36px;
      font-weight: bold;
      border: none;
      padding: 10px 15px;
      cursor: pointer;
      display: none;
      user-select: none;
    }

    iframe {
      width: 90vw;
      height: 80vh;
      border: none;
      border-radius: 12px;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <button id="backBtn" onclick="goBack()">←</button>
  <h1>Just a Tiiny Emulator</h1>
  <div class="button-container" id="menu">
    <button onclick="loadSite('https://math-fire.vercel.app/main/play/testlaunch.html')">Upload your own ROM</button>
    <button onclick="loadSite('https://gba-emulator-livid.vercel.app/')">Select ROM</button>
    <button onclick="downloadFreshHTML()">Download Emulator</button>
  </div>
  <iframe id="siteFrame" style="display:none;"></iframe>

  <script>
    function loadSite(url) {
      const iframe = document.getElementById("siteFrame");
      iframe.src = url;
      iframe.style.display = "block";
      document.getElementById("menu").style.display = "none";
      document.getElementById("backBtn").style.display = "block";
    }

    function goBack() {
      const iframe = document.getElementById("siteFrame");
      iframe.style.display = "none";
      iframe.src = "";
      document.getElementById("menu").style.display = "flex";
      document.getElementById("backBtn").style.display = "none";
    }

    function downloadFreshHTML() {
      const fullHTML = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Just a Tiiny Emulator</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #121212;
      color: white;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      height: 100vh;
      text-align: center;
    }

    h1 {
      font-size: 28px;
      margin-top: 60px;
    }

    .button-container {
      display: flex;
      flex-direction: column;
      gap: 15px;
      margin-top: 30px;
    }

    button {
      padding: 15px 30px;
      font-size: 18px;
      border: none;
      border-radius: 10px;
      background: #1e90ff;
      color: white;
      cursor: pointer;
      transition: background 0.3s;
    }

    button:hover {
      background: #0d74d1;
    }

    #backBtn {
      position: absolute;
      top: 0;
      left: 0;
      background: transparent;
      color: white;
      font-size: 36px;
      font-weight: bold;
      border: none;
      padding: 10px 15px;
      cursor: pointer;
      display: none;
      user-select: none;
    }

    iframe {
      width: 90vw;
      height: 80vh;
      border: none;
      border-radius: 12px;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <button id="backBtn" onclick="goBack()">←</button>
  <h1>Just a Tiiny Emulator</h1>
  <div class="button-container" id="menu">
    <button onclick="loadSite('https://math-fire.vercel.app/main/play/testlaunch.html')">Upload your own ROM</button>
    <button onclick="loadSite('https://gba-emulator-livid.vercel.app/')">Select ROM</button>
  </div>
  <iframe id="siteFrame" style="display:none;"></iframe>

  <script>
    function loadSite(url) {
      const iframe = document.getElementById('siteFrame');
      iframe.src = url;
      iframe.style.display = 'block';
      document.getElementById('menu').style.display = 'none';
      document.getElementById('backBtn').style.display = 'block';
    }

    function goBack() {
      const iframe = document.getElementById('siteFrame');
      iframe.style.display = 'none';
      iframe.src = '';
      document.getElementById('menu').style.display = 'flex';
      document.getElementById('backBtn').style.display = 'none';
    }
  <\/script>
</body>
</html>`;

      const blob = new Blob([fullHTML], { type: 'text/html' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = 'tiiny-emulator.html';
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    }
  </script>
</body>
</html>
